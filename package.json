{
  "name": "credly-scraper",
  "version": "1.0.0",
  "description": "Credly badge scraper with profile management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "lint": "node --check server.js && node --check script.js && node --check predefined-profiles.js && node --check badge-utils.js && bash -n credly_badge_downloader.sh"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
